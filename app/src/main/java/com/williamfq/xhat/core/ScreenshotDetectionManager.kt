
package com.williamfq.xhat.core

class ScreenshotDetectionManager {
    fun notifyScreenshotTaken(userId: String, contentId: String) {
        println("Captura de pantalla detectada por el usuario \$userId en el contenido \$contentId.")
    }
}
