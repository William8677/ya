
package com.williamfq.xhat.core

class BotsManager {
    fun createReminderBot(groupId: String, reminder: String) {
        // Crear un bot que envíe recordatorios automáticos a un grupo
        println("Bot recordatorio creado para el grupo \$groupId con el mensaje: \$reminder")
    }
}
