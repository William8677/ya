
package com.williamfq.xhat.core

class EventScheduler {
    fun scheduleEvent(eventId: String, timestamp: Long) {
        println("Evento \$eventId programado para la fecha y hora \$timestamp.")
    }
}
